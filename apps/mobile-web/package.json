{
  "name": "@dsh-mobile/mobile-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Android-first Capacitor shell with camera pairing and direct WebRTC tunnel",
  "license": "MIT",
  "exports": {
    "./dist/*": "./dist/*",
    "./package.json": "./package.json"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "node ../../scripts/prepare-upstream.mjs && node scripts/build-local-host-bridge.mjs && npm run build --workspace @dsh-mobile/interaction-operations && npm run build --workspace @dsh-mobile/ui-layout-mobile && vite build && node scripts/verify-shell-module-boundary.mjs && node scripts/package-local-shell.mjs",
    "test": "node --test test/*.test.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "watch": "vite build --watch",
    "android:copy": "npm run build && cap copy android && node scripts/package-android-layout.mjs",
    "android:sync": "npm run build && cap sync android && node scripts/package-android-layout.mjs",
    "android:debug": "bash android/build-debug.sh",
    "android:release": "bash android/build-release.sh",
    "android:acceptance": "node scripts/run-physical-acceptance.mjs"
  },
  "dependencies": {
    "@capacitor/android": "^8.5.0",
    "@capacitor/app": "^8.0.1",
    "@capacitor/barcode-scanner": "^3.1.1",
    "@capacitor/core": "^8.5.0",
    "@dsh-mobile/e2e-tunnel": "*",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.5.0",
    "@vitejs/plugin-react": "^4.0.0",
    "typescript": "^6.0.3",
    "vite": "^6.0.0"
  }
}
