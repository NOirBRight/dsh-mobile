bindPort = 7000
vhostHTTPPort = 8080
# Auth token is injected at container start from FRP_TOKEN (compose fails if unset or replace-me).
auth.method = "token"
auth.token = "unused-compose-overrides-this"
